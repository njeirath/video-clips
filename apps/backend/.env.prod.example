# Example AWS credentials file for local/testing only.
# DO NOT COMMIT real credentials. Copy this file to `.env.prod.local` and fill
# the values for local use. Add `.env.prod.local` to .gitignore.

# AWS credentials (example placeholders)
AWS_ACCESS_KEY_ID=AKIAEXAMPLE
AWS_SECRET_ACCESS_KEY=examplesecretkey
AWS_REGION=us-east-1

# Optional: specify a profile if you mount ~/.aws and want to use a non-default profile
# AWS_PROFILE=default

# OpenSearch protocol override (ensure this matches your OpenSearch setup)
# OPENSEARCH_PROTOCOL=http
