# GraphQL Codegen configuration for frontend
schema: ./schema.graphql
# Adjust the path above if your backend runs elsewhere

documents: "apps/frontend/src/**/*.tsx"
generates:
  apps/frontend/src/gql/:
    preset: client
